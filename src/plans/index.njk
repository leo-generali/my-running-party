---
layout: layouts/main.njk
---

<section class='mt-8 mb-12'>
  <h1 class='font-bold text-4xl'>Running Training Plans</h1>
</section>

<section class='grid grid-cols-2 gap-4'>
{% for plan in plans %}
  {% set name = plan.name %}
  {% set slug = '/plans/' + name | slug %}
    <a 
      class='
        group transform relative overflow-hidden
        bg-lime-500 transition duration-75
        shadow p-4 rounded-lg block text-shadow
        hover:bg-lime-400 hover:-translate-y-1 hover:shadow-xl
        active:translate-y-px active:bg-lime-600'
      href="{{ slug }}">
      <p 
        class='
          text-lime-200 font-semibold tracking-widest 
          uppercase group-hover:text-lime-100'>
        {{ name }}
      </p>
      <p class='mt-4 text-white'>
        {{ plan.description.short | safe }}
      </p>
    </a>
  {% endfor %}
</section>